{
  "watch": ["src"],
  "ext": "ts",
  "ignore": [],
  "exec": "tsc --project tsconfig.build.json && resolve-tspaths && homebridge -I -D",
  "signal": "SIGTERM",
  "env": {
    "NODE_OPTIONS": "--trace-warnings"
  }
}
